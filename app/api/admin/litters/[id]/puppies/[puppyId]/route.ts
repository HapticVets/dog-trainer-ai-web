import { NextResponse } from "next/server";
import { requireAdmin } from "@/lib/admin";
import { supabaseAdmin } from "@/lib/supabase-admin";
type Ctx={params:Promise<{id:string;puppyId:string}>};
export async function DELETE(_r:Request,{params}:Ctx){try{await requireAdmin();const{id,puppyId}=await params;const{data:puppy,error:lookupError}=await supabaseAdmin.from("admin_litter_puppies").select("id,profile_image_path").eq("id",puppyId).eq("litter_id",id).maybeSingle();if(lookupError||!puppy)return NextResponse.json({success:false,error:"Puppy record not found."},{status:404});for(const table of ["admin_puppy_weights","admin_puppy_notes"]){const{error}=await supabaseAdmin.from(table).delete().eq("puppy_id",puppyId);if(error){console.error("Admin puppy child cleanup failed",{table,code:error.code,message:error.message});return NextResponse.json({success:false,error:"Unable to remove related puppy records."},{status:500});}}const{data,error}=await supabaseAdmin.from("admin_litter_puppies").delete().eq("id",puppyId).eq("litter_id",id).select("id").maybeSingle();if(error||!data)return NextResponse.json({success:false,error:"Unable to delete puppy."},{status:500});return NextResponse.json({success:true,photoCleanupPending:Boolean(puppy.profile_image_path)});}catch(error){console.error("Admin puppy delete failed",error);return NextResponse.json({success:false,error:"Unable to delete puppy."},{status:500});}}
