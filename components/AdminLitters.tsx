"use client";
import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import DeleteLitterButton from "@/components/DeleteLitterButton";
type Dog={id:string;name:string};type Litter={id:string;litter_code:string;name:string;status:string};
export default function AdminLitters(){const[litters,setLitters]=useState<Litter[]>([]);const[dogs,setDogs]=useState<Dog[]>([]);const[error,setError]=useState("");const[form,setForm]=useState({litter_code:"",name:"",sire_dog_id:"",dam_dog_id:"",status:"planned"});useEffect(()=>{void(async()=>{const r=await fetch("/api/admin/litters");const d=await r.json();if(!r.ok){setError(d.error);return;}setLitters(d.litters);setDogs(d.breedingDogs)})()},[]);const submit=async(e:FormEvent)=>{e.preventDefault();const r=await fetch("/api/admin/litters",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();if(!r.ok){setError(d.error);return;}setLitters(x=>[d.litter,...x])};return <main className="mx-auto max-w-6xl p-4 sm:p-6"><Link href="/admin" className="text-sm text-amber-200">Back to Admin</Link><h1 className="mt-4 text-3xl font-bold text-white">Litters</h1><form onSubmit={submit} className="mt-6 grid gap-3 rounded-2xl border border-neutral-800 bg-neutral-950 p-5 sm:grid-cols-2"><input required placeholder="Litter code" value={form.litter_code} onChange={e=>setForm({...form,litter_code:e.target.value})} className="rounded bg-black p-3"/><input required placeholder="Litter name / theme" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="rounded bg-black p-3"/><select value={form.sire_dog_id} onChange={e=>setForm({...form,sire_dog_id:e.target.value})} className="rounded bg-black p-3"><option value="">Select sire</option>{dogs.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><select value={form.dam_dog_id} onChange={e=>setForm({...form,dam_dog_id:e.target.value})} className="rounded bg-black p-3"><option value="">Select dam</option>{dogs.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><button className="rounded bg-amber-400 p-3 font-bold text-black">Create Litter</button></form>{error&&<p className="mt-3 text-red-300">{error}</p>}<div className="mt-6 grid gap-3 sm:grid-cols-2">{litters.map(l=><article key={l.id} className="rounded-xl border border-neutral-800 bg-neutral-950 p-4"><p className="text-amber-300">{l.litter_code}</p><h2 className="font-bold text-white">{l.name}</h2><p className="text-sm text-neutral-400">{l.status}</p><div className="mt-4 flex gap-2"><Link href={`/admin/litters/${l.id}`} className="rounded-lg border border-amber-400/35 px-3 py-2 text-sm font-semibold text-amber-100">Open Litter</Link><DeleteLitterButton litterId={l.id} label="Delete" onDeleted={()=>setLitters(x=>x.filter(item=>item.id!==l.id))}/></div></article>)}</div></main>}
