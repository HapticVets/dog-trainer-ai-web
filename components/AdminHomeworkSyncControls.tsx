"use client";

export type HomeworkSyncValue = { enabled: boolean; focus: string; notes: string };

export default function AdminHomeworkSyncControls({ value, onChange }: { value: HomeworkSyncValue; onChange: (value: HomeworkSyncValue) => void }) {
  return <section className="rounded-2xl border border-amber-400/25 bg-amber-400/5 p-5 sm:p-6"><p className="text-xs font-bold uppercase tracking-[0.2em] text-amber-300">Client Homework Sync</p><h2 className="mt-2 text-xl font-bold text-white">Approved customer guidance</h2><label className="mt-4 flex items-start gap-3 text-sm text-neutral-200"><input type="checkbox" checked={value.enabled} onChange={(event) => onChange({ ...value, enabled: event.target.checked })} className="mt-1 h-4 w-4 accent-amber-400" />Use the completed professional session to guide client homework.</label>{value.enabled && <div className="mt-4 space-y-3"><label className="block text-sm font-semibold text-neutral-200">Client Homework Focus<textarea value={value.focus} onChange={(event) => onChange({ ...value, focus: event.target.value })} rows={3} className="mt-2 w-full rounded-lg border border-neutral-700 bg-black/40 px-3 py-2.5 text-white" /></label><label className="block text-sm font-semibold text-neutral-200">Client Homework Notes <span className="font-normal text-neutral-500">(optional, customer-safe)</span><textarea value={value.notes} onChange={(event) => onChange({ ...value, notes: event.target.value })} rows={3} className="mt-2 w-full rounded-lg border border-neutral-700 bg-black/40 px-3 py-2.5 text-white" /></label></div>}</section>;
}
