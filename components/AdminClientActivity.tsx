import type { AdminClientActivitySummary } from "@/lib/adminClientActivity";

const formatDate = (value: string | null) => value
  ? new Intl.DateTimeFormat("en-US", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value))
  : "Not recorded";

const lastSessionLabel = (summary: AdminClientActivitySummary) => {
  if (!summary.lastClientSession) return "No client sessions logged yet";
  const days = summary.daysSinceLastClientSession ?? 0;
  return days === 0 ? "Today" : days === 1 ? "1 day ago" : `${days} days ago`;
};

export default function AdminClientActivity({ summary, compact = false }: { summary: AdminClientActivitySummary | undefined; compact?: boolean }) {
  if (!summary?.available) return compact
    ? <p className="mt-3 text-xs text-neutral-500">Client activity unavailable until linked.</p>
    : <section className="rounded-2xl border border-neutral-800 bg-neutral-950/80 p-5 sm:p-6"><p className="text-xs font-bold uppercase tracking-[0.2em] text-amber-300">Client Training Activity</p><h2 className="mt-2 text-2xl font-bold text-white">Linked account activity</h2><p className="mt-4 text-sm leading-6 text-neutral-400">Client activity unavailable until the client account and dog profile are linked.</p></section>;

  if (compact) return <p className="mt-3 text-xs text-neutral-400">{summary.activityStatus}{summary.lastClientSession ? ` · Last client session: ${lastSessionLabel(summary)}` : ""}</p>;

  return <section className="rounded-2xl border border-neutral-800 bg-neutral-950/80 p-5 sm:p-6"><p className="text-xs font-bold uppercase tracking-[0.2em] text-amber-300">Client Training Activity</p><div className="mt-2 flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-2xl font-bold text-white">Client follow-through</h2><p className="mt-1 text-sm text-neutral-400">{summary.activityStatus}</p></div><p className="rounded-full border border-neutral-700 px-3 py-1 text-xs font-semibold text-neutral-200">Last Trainer activity: {formatDate(summary.lastTrainerActivityAt)}</p></div><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><div className="rounded-xl border border-neutral-800 bg-black/30 p-3"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-neutral-500">Last client session</p><p className="mt-1 text-sm font-semibold text-white">{lastSessionLabel(summary)}</p>{summary.lastClientSession?.focus && <p className="mt-1 text-xs text-neutral-400">{summary.lastClientSession.focus}</p>}</div><div className="rounded-xl border border-neutral-800 bg-black/30 p-3"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-neutral-500">Sessions last 7 days</p><p className="mt-1 text-xl font-bold text-white">{summary.sessionsLast7Days}</p></div><div className="rounded-xl border border-neutral-800 bg-black/30 p-3"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-neutral-500">Sessions last 30 days</p><p className="mt-1 text-xl font-bold text-white">{summary.sessionsLast30Days}</p></div><div className="rounded-xl border border-neutral-800 bg-black/30 p-3"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-neutral-500">Days since session</p><p className="mt-1 text-xl font-bold text-white">{summary.daysSinceLastClientSession ?? "-"}</p></div></div><div className="mt-5 grid gap-4 lg:grid-cols-2"><div className="rounded-xl border border-neutral-800 bg-black/30 p-4"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-neutral-500">Current homework</p><p className="mt-2 text-sm text-neutral-100">{summary.currentHomework?.focus || "No active homework assigned"}</p>{summary.currentHomework && <p className="mt-2 text-xs text-neutral-500">Assigned {formatDate(summary.currentHomework.assignedAt)}</p>}</div><div className="rounded-xl border border-neutral-800 bg-black/30 p-4"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-neutral-500">Most recent result</p><p className="mt-2 text-sm leading-6 text-neutral-200">{summary.lastClientSession?.result || "No session result recorded yet."}</p></div></div><div className="mt-5 border-t border-neutral-800 pt-5"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-amber-200">Recent client activity</p>{summary.recentActivities.length ? <ol className="mt-3 space-y-3">{summary.recentActivities.map((activity, index) => <li key={`${activity.occurredAt}-${activity.title}-${index}`} className="flex gap-3 text-sm"><time className="w-24 shrink-0 text-xs text-neutral-500">{formatDate(activity.occurredAt)}</time><p className="text-neutral-200"><span className="font-semibold text-white">{activity.title}</span>{activity.detail ? ` - ${activity.detail}` : ""}</p></li>)}</ol> : <p className="mt-3 text-sm text-neutral-500">No client training activity recorded yet.</p>}</div></section>;
}
