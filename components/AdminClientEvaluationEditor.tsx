"use client";

import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { createId, emptyConfig, fieldTypeLabels, generalServiceDogProgressConfig, type EvaluationConfig, type EvaluationFieldType, type EvaluationResults } from "@/lib/adminClientEvaluations";

type Template = { id: string; title: string; description: string | null; config: EvaluationConfig };
type Evaluation = { id: string; title: string; template_id: string | null; template_name: string | null; evaluation_date: string; config_snapshot: EvaluationConfig; results: EvaluationResults; trainer_summary: string | null };
const types = Object.keys(fieldTypeLabels) as EvaluationFieldType[];
const inputClasses = "mt-1 min-h-11 w-full rounded-lg border border-neutral-700 bg-black px-3 py-2 text-white outline-none focus:border-amber-400";

function FieldInput({ field, value, onChange }: { field: { id: string; label: string; type: EvaluationFieldType }; value: string | number | boolean | null | undefined; onChange: (value: string | number | boolean) => void }) {
  if (field.type === "rating") return <select value={typeof value === "number" || typeof value === "string" ? value : ""} onChange={(event) => onChange(event.target.value ? Number(event.target.value) : "")} className={inputClasses}><option value="">Not observed</option><option value="1">1 - Significant support needed</option><option value="2">2 - Developing</option><option value="3">3 - Functional / moderate</option><option value="4">4 - Strong</option><option value="5">5 - Very strong</option></select>;
  if (field.type === "pass_needs_work") return <select value={String(value ?? "")} onChange={(event) => onChange(event.target.value)} className={inputClasses}><option value="">Not observed</option><option>Pass</option><option>Needs Work</option></select>;
  if (field.type === "yes_no") return <select value={String(value ?? "")} onChange={(event) => onChange(event.target.value)} className={inputClasses}><option value="">Not observed</option><option>Yes</option><option>No</option></select>;
  return field.type === "long_notes" ? <textarea rows={4} value={String(value ?? "")} onChange={(event) => onChange(event.target.value)} className={inputClasses} /> : <input value={String(value ?? "")} onChange={(event) => onChange(event.target.value)} className={inputClasses} />;
}

export default function AdminClientEvaluationEditor({ dogId, evaluationId }: { dogId: string; evaluationId?: string }) {
  const [templates, setTemplates] = useState<Template[]>([]); const [config, setConfig] = useState<EvaluationConfig>(emptyConfig); const [results, setResults] = useState<EvaluationResults>({}); const [title, setTitle] = useState(""); const [date, setDate] = useState(() => new Date().toISOString().slice(0, 10)); const [template, setTemplate] = useState<Template | null>(null); const [summary, setSummary] = useState(""); const [mode, setMode] = useState<"choose" | "edit">(evaluationId ? "edit" : "choose"); const [loading, setLoading] = useState(Boolean(evaluationId)); const [saving, setSaving] = useState(false); const [generating, setGenerating] = useState(false); const [error, setError] = useState(""); const [notice, setNotice] = useState("");
  useEffect(() => { void (async () => { try { const templatesResponse = await fetch("/api/admin/evaluation-templates", { cache: "no-store" }); const templatesBody = await templatesResponse.json(); if (!templatesResponse.ok) throw new Error(templatesBody.error || "Unable to load templates."); setTemplates(templatesBody.templates ?? []); if (evaluationId) { const response = await fetch(`/api/admin/dogs/${dogId}/evaluations/${evaluationId}`, { cache: "no-store" }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Unable to load evaluation."); const evaluation = body.evaluation as Evaluation; setTitle(evaluation.title); setDate(evaluation.evaluation_date.slice(0, 10)); setConfig(evaluation.config_snapshot); setResults(evaluation.results ?? {}); setSummary(evaluation.trainer_summary ?? ""); setTemplate((templatesBody.templates ?? []).find((item: Template) => item.id === evaluation.template_id) ?? null); } } catch (loadError) { setError(loadError instanceof Error ? loadError.message : "Unable to load evaluation."); } finally { setLoading(false); } })(); }, [dogId, evaluationId]);
  const updateSection = (sectionId: string, update: Partial<EvaluationConfig["sections"][number]>) => setConfig((current) => ({ sections: current.sections.map((section) => section.id === sectionId ? { ...section, ...update } : section) }));
  const addSection = () => setConfig((current) => ({ sections: [...current.sections, { id: createId(), title: "New Section", notes: false, fields: [] }] }));
  const addField = (sectionId: string) => updateSection(sectionId, { fields: [...(config.sections.find((section) => section.id === sectionId)?.fields ?? []), { id: createId(), label: "New Field", type: "rating" }] });
  const updateField = (sectionId: string, fieldId: string, update: Record<string, unknown>) => updateSection(sectionId, { fields: (config.sections.find((section) => section.id === sectionId)?.fields ?? []).map((field) => field.id === fieldId ? { ...field, ...update } : field) });
  const save = async (event: FormEvent) => { event.preventDefault(); if (!title.trim()) { setError("Evaluation title is required."); return; } setSaving(true); setError(""); try { const payload = { title: title.trim(), evaluation_date: new Date(date).toISOString(), template_id: template?.id ?? null, template_name: template?.title ?? null, config_snapshot: config, results, trainer_summary: summary.trim() || null }; const response = await fetch(evaluationId ? `/api/admin/dogs/${dogId}/evaluations/${evaluationId}` : `/api/admin/dogs/${dogId}/evaluations`, { method: evaluationId ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Unable to save evaluation."); setNotice("Evaluation saved."); if (!evaluationId) window.location.assign(`/admin/dogs/${dogId}/evaluations/${body.evaluation.id}`); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : "Unable to save evaluation."); } finally { setSaving(false); } };
  const generateSummary = async () => { if (!evaluationId) { setError("Save the evaluation before generating a summary."); return; } setGenerating(true); setError(""); try { const response = await fetch(`/api/admin/dogs/${dogId}/evaluations/${evaluationId}/summary`, { method: "POST" }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Unable to generate a summary."); setSummary(body.summary); setNotice("Summary draft generated. Review it and save the evaluation."); } catch (summaryError) { setError(summaryError instanceof Error ? summaryError.message : "Unable to generate a summary."); } finally { setGenerating(false); } };
  const saveTemplate = async () => { const templateTitle = window.prompt("Template name"); if (!templateTitle?.trim()) return; try { const response = await fetch("/api/admin/evaluation-templates", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ title: templateTitle.trim(), config }) }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Unable to save template."); setTemplates((current) => [body.template, ...current]); setTemplate(body.template); setNotice("Evaluation saved as a reusable template."); } catch (templateError) { setError(templateError instanceof Error ? templateError.message : "Unable to save template."); } };
  if (loading) return <p className="mx-auto max-w-5xl p-8 text-neutral-300">Loading evaluation...</p>;
  if (mode === "choose") return <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6"><Link href={`/admin/dogs/${dogId}`} className="text-sm font-semibold text-amber-200">Back to Client Dog</Link><section className="mt-5 rounded-2xl border border-amber-400/20 bg-neutral-950 p-5 sm:p-8"><p className="text-xs font-bold uppercase tracking-[0.2em] text-amber-300">New Client Dog Evaluation</p><h1 className="mt-2 text-3xl font-bold text-white">Choose a starting point</h1><div className="mt-6 grid gap-4 sm:grid-cols-2"><button type="button" onClick={() => { setConfig(emptyConfig()); setTemplate(null); setMode("edit"); }} className="rounded-xl border border-neutral-700 p-5 text-left text-white hover:border-amber-400/50"><strong>Build Custom Evaluation</strong><span className="mt-2 block text-sm text-neutral-400">Create sections and observation fields for this dog&apos;s current work.</span></button><button type="button" onClick={() => { setConfig(generalServiceDogProgressConfig()); setTemplate(null); setTitle("General / Service Dog Progress Evaluation"); setMode("edit"); }} className="rounded-xl border border-amber-400/30 p-5 text-left text-white hover:bg-amber-400/5"><strong>Use Henry Example Structure</strong><span className="mt-2 block text-sm text-neutral-400">Start with a practical general/service-dog progress evaluation, then customize it.</span></button></div>{templates.length > 0 && <div className="mt-6"><h2 className="font-bold text-white">Use Saved Template</h2><div className="mt-3 grid gap-3 sm:grid-cols-2">{templates.map((item) => <button key={item.id} type="button" onClick={() => { setConfig(item.config); setTemplate(item); setTitle(item.title); setMode("edit"); }} className="rounded-xl border border-neutral-800 bg-black/30 p-4 text-left text-white hover:border-amber-400/40"><strong>{item.title}</strong><span className="mt-1 block text-sm text-neutral-400">{item.description || "Reusable evaluation template"}</span></button>)}</div></div>}</section></main>;
  return <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6"><Link href={`/admin/dogs/${dogId}`} className="text-sm font-semibold text-amber-200">Back to Client Dog</Link><form onSubmit={save} className="mt-5 space-y-5"><header className="rounded-2xl border border-amber-400/20 bg-neutral-950 p-5"><p className="text-xs font-bold uppercase tracking-[0.2em] text-amber-300">{evaluationId ? "Client Dog Evaluation" : "New Client Dog Evaluation"}</p><div className="mt-4 grid gap-3 sm:grid-cols-[1fr_12rem]"><label className="text-sm text-neutral-200">Evaluation title<input required value={title} onChange={(event) => setTitle(event.target.value)} className={inputClasses} /></label><label className="text-sm text-neutral-200">Evaluation date<input required type="date" value={date} onChange={(event) => setDate(event.target.value)} className={inputClasses} /></label></div></header>{config.sections.map((section) => <section key={section.id} className="rounded-2xl border border-neutral-800 bg-neutral-950 p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><label className="flex-1 text-sm text-neutral-200">Section title<input value={section.title} onChange={(event) => updateSection(section.id, { title: event.target.value })} className={inputClasses} /></label><div className="flex gap-2"><button type="button" onClick={() => addField(section.id)} className="min-h-11 rounded-lg border border-amber-400/35 px-3 text-sm font-semibold text-amber-100">Add Field</button><button type="button" onClick={() => setConfig((current) => ({ sections: current.sections.filter((candidate) => candidate.id !== section.id) }))} className="min-h-11 rounded-lg border border-red-400/30 px-3 text-sm font-semibold text-red-200">Delete Section</button></div></div><div className="mt-4 space-y-4">{section.fields.map((field) => <div key={field.id} className="rounded-xl border border-neutral-800 bg-black/30 p-3"><div className="grid gap-3 sm:grid-cols-[1fr_13rem_auto]"><input value={field.label} onChange={(event) => updateField(section.id, field.id, { label: event.target.value })} aria-label="Field label" className={inputClasses.replace("mt-1 ", "")} /><select value={field.type} onChange={(event) => updateField(section.id, field.id, { type: event.target.value as EvaluationFieldType })} className={inputClasses.replace("mt-1 ", "")} >{types.map((type) => <option key={type} value={type}>{fieldTypeLabels[type]}</option>)}</select><button type="button" onClick={() => updateSection(section.id, { fields: section.fields.filter((candidate) => candidate.id !== field.id) })} className="min-h-11 rounded-lg border border-red-400/30 px-3 text-sm text-red-200">Remove</button></div><label className="mt-3 block text-sm text-neutral-200">{field.label || "Observation"}<FieldInput field={field} value={results[field.id]} onChange={(value) => setResults((current) => ({ ...current, [field.id]: value }))} /></label></div>)}</div>{section.notes && <label className="mt-4 block text-sm text-neutral-200">Section notes<textarea rows={3} value={String(results[`${section.id}:notes`] ?? "")} onChange={(event) => setResults((current) => ({ ...current, [`${section.id}:notes`]: event.target.value }))} className={inputClasses} /></label>}</section>)}<button type="button" onClick={addSection} className="min-h-11 rounded-lg border border-amber-400/35 px-4 font-semibold text-amber-100">Add Section</button><section className="rounded-2xl border border-neutral-800 bg-neutral-950 p-5"><div className="flex flex-wrap gap-3"><button type="button" onClick={() => void saveTemplate()} className="min-h-11 rounded-lg border border-neutral-700 px-4 font-semibold text-white">Save As Template</button>{evaluationId && <button type="button" disabled={generating} onClick={() => void generateSummary()} className="min-h-11 rounded-lg border border-amber-400/35 px-4 font-semibold text-amber-100">{generating ? "Generating..." : "Generate Evaluation Summary"}</button>}</div><label className="mt-5 block text-sm text-neutral-200">Internal professional summary<textarea rows={9} value={summary} onChange={(event) => setSummary(event.target.value)} placeholder="Optional. Generate a draft from saved observations, then review it before saving." className={inputClasses} /></label></section>{notice && <p className="text-sm text-emerald-200" role="status">{notice}</p>}{error && <p className="text-sm text-red-200" role="alert">{error}</p>}<div className="sticky bottom-3 rounded-xl border border-amber-400/25 bg-neutral-950/95 p-3 shadow-xl"><button disabled={saving} className="min-h-11 w-full rounded-lg bg-amber-400 px-4 font-bold text-black disabled:opacity-60">{saving ? "Saving..." : "Save Evaluation"}</button></div></form></main>;
}
